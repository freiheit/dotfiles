# Only the settings that differ from gh's defaults. gh rewrites this file with
# its full annotated template whenever it changes a setting itself, so expect to
# re-trim after `gh config set`.
version: 1
aliases:
    co: pr checkout
